# Experiment 08 — React Native New Architecture

## Objective

Enable and verify React Native's New Architecture and confirm Fabric is active at runtime.

## Configuration

The project uses React Native 0.86.2 with the New Architecture enabled.

The Android configuration contains:

```properties
newArchEnabled=true
hermesEnabled=true
edgeToEdgeEnabled=true
